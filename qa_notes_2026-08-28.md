# 홈페이지 최종 문구·노출 QA 기록

- 데스크톱 `/` 캡처에서 헤더·상단 검색·히어로·6개 카테고리·추천 생활정보·최신 콘텐츠·생활 계산기·운영 원칙·Footer 순서가 확인됨.
- 추천 섹션 설명에서 방문 통계 관련 내부 운영 문구가 제거됨.
- 기존 히어로 내부 추천 블록을 제거해 중복 노출을 줄이고, 추천 생활정보를 카테고리 다음의 대표 글 영역으로 일원화함.
- 최신 콘텐츠는 4개 항목으로 간결하게 노출되며 전체 글 목록 링크가 유지됨.
- 생활 계산기 섹션은 실제 계산기 3개 링크와 전체 계산기 링크를 제공함.
- 콘텐츠 제목에 한 달 생활비, 기초연금, 자동차 연간 유지비, 엔진오일, 에어컨 물샘, 여행 예상비용 등 실제 본문과 맞는 검색 핵심어를 반영함.
- 모바일 375px 전체 페이지 캡처에서 카드가 한 열로 전환되고 제목·본문이 영역 밖으로 넘치지 않으며 Footer까지 정상 표시됨.
- 다음 검증: `pnpm check`, `pnpm build:cloudflare`, 정적 HTML/메타·Sitemap 확인 후 체크포인트 저장 및 GitHub main 푸시.

## 추가 확인

최신 데스크톱 캡처에서는 공통 헤더 검색 아래에 간결한 소개 블록과 6개 카테고리가 바로 이어지고, 추천 생활정보·최신 콘텐츠·생활 계산기·운영 원칙·Footer 순서가 유지되었다. 기존 큰 히어로 이미지와 두 번째 검색 폼은 제거되었지만 생활비랩의 큰 제목, 테라코타 밑줄, 딥 블루 추천 영역, 카드형 편집 리듬은 유지되었다.

최신 375px 모바일 캡처에서는 공통 검색이 한 번만 노출되고 소개·카테고리·추천 카드·최신 목록·계산기 카드가 세로로 안정적으로 배치되었다. 제목과 설명은 카드 영역 밖으로 넘치지 않았고, 하단 운영 원칙과 Footer까지 빈 영역 없이 표시되었다.

## 최종 순서 확인

최신 데스크톱·모바일 캡처에서 홈페이지의 실제 시각 순서가 `헤더 → 검색 스트립 → 6개 카테고리 카드 → 추천 생활정보 → 최신 콘텐츠 → 생활 계산기 → 운영 원칙 → Footer`로 완벽히 정렬되었다. 기존의 별도 소개 블록(`home-intro`)을 제거하고 브랜드 메시지를 카테고리 섹션 상단 설명으로 흡수하여, 사용자가 처음 방문했을 때 불필요한 중복 정보 없이 바로 분야를 선택할 수 있도록 간결해졌다.

## 정적 DOM 감사

최종 `dist/public/index.html`에서 `header-search-bar`, `category-section`, `featured-section`, `latest-section`, `calculator-section`, `trust-section` 클래스의 위치를 직접 비교했다. 위치가 모두 존재하고 순서가 `header-search-bar → category-section → featured-section → latest-section → calculator-section → trust-section`로 이어져 `dom_order=PASS`였으며, `<main>`은 1개였다.

## 최종 카드 스타일 보완 검수

계산기 목록 카드까지 제목 2줄·설명 3줄 클램프와 `h-full`을 적용했다. 데스크톱과 모바일 캡처에서 홈페이지 카테고리·추천 게시글·생활 계산기 카드가 동일한 카드 문법으로 보이고, 1.5rem 수준의 간격·패딩, 연한 테두리와 은은한 hover용 shadow가 반영되었다. 모바일에서는 카드가 한 열로 전환되고 콘텐츠가 카드 밖으로 넘치지 않았다. 헤더의 6개 메뉴와 검색 버튼도 한 줄 및 모바일 축약 상태를 유지했다.

## 최종 모바일 카드 재검수

최종 카드 보강 이후 375px 모바일에서 `/`와 `/calculators`를 다시 캡처했다. 카테고리·추천 게시글·계산기 카드가 한 열 또는 지정된 그리드로 안정적으로 배치되었고, 제목·설명 클램프와 `h-full`, 1.5rem 패딩·간격, 연한 테두리와 은은한 그림자가 유지되었다. 화면 바깥으로 튀어나오는 텍스트와 가로 스크롤은 확인되지 않았다.

## 계산기 링크 정렬 최종 검수

계산기 카드의 공통 직계 `span` 원형 규칙을 번호 배지와 액션 링크에 분리했다. 최종 375px 모바일 캡처의 홈페이지와 계산기 목록에서 번호만 원형으로 표시되고 `계산해 보기`·`계산기 열기`는 원형 테두리 없이 아이콘과 함께 한 줄로 정렬되었다. 텍스트 겹침과 가로 넘침은 확인되지 않았다.

## GitHub 자동 배포 권한 점검

GitHub `gjh4842-cyber/life-cost-lab`의 Rulesets 화면에는 생성된 규칙이 없고, Branches 화면에도 classic branch protection이 설정되어 있지 않다. Collaborators and teams 화면에는 “0 collaborators have access. Only you can contribute to this repository”라고 표시되므로 소유자 계정의 저장소 접근 자체는 정상이다. 그럼에도 Git push와 branch protection API가 통합 인증에서 403을 반환하므로, 현재 문제는 main 보호 규칙이 아니라 GitHub CLI/통합 인증의 쓰기 토큰 범위 또는 연결 상태로 판단된다.

## 2026-08-28 로고 수정 배포 확인

- GitHub `main` 원격 파일에 기존 `/manus-storage/trusted-life-guide-mark_0491c4bb.png` 참조가 남아 있던 것을 확인해, `SiteHeader.tsx`를 외부 이미지 없이 `랩` 텍스트 마크를 렌더링하도록 수정했다.
- `index.css`에 `.brand__mark`의 고정 크기·원형·배경·색상·모바일 축소 규칙을 추가했다.
- 로컬 `tsc --noEmit` 및 Vitest 통과, `pnpm build:cloudflare` 통과(정적 사이트맵 31개 URL, 콘텐츠 라우트 34개 사전 렌더링).
- GitHub Contents API 동기화 후 원격 `main` 커밋: `cf659e6bbfc7584b611903b3ac89d907ffb87b6b`.
- 배포 직후 `https://life-cost-lab-site.pages.dev/` 재조회에서는 기존 로고 이미지가 계속 보였으므로 Cloudflare Pages의 GitHub 자동 배포 반영 지연 가능성이 있다. 새 배포 완료 후 홈과 `/calculators`를 다시 직접 확인해야 한다.
- 홈의 섹션 순서는 헤더 → 검색 → 6개 카테고리 → 추천 생활정보 → 최신 콘텐츠 → 생활 계산기 → 운영 원칙으로 확인했다.
