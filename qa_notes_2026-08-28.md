# 홈페이지 최종 문구·노출 QA 기록

- 데스크톱 `/` 캡처에서 헤더·상단 검색·히어로·6개 카테고리·추천 생활정보·최신 콘텐츠·생활 계산기·운영 원칙·Footer 순서가 확인됨.
- 추천 섹션 설명에서 방문 통계 관련 내부 운영 문구가 제거됨.
- 기존 히어로 내부 추천 블록을 제거해 중복 노출을 줄이고, 추천 생활정보를 카테고리 다음의 대표 글 영역으로 일원화함.
- 최신 콘텐츠는 4개 항목으로 간결하게 노출되며 전체 글 목록 링크가 유지됨.
- 생활 계산기 섹션은 실제 계산기 3개 링크와 전체 계산기 링크를 제공함.
- 콘텐츠 제목에 한 달 생활비, 기초연금, 자동차 연간 유지비, 엔진오일, 에어컨 물샘, 여행 예상비용 등 실제 본문과 맞는 검색 핵심어를 반영함.
- 모바일 375px 전체 페이지 캡처에서 카드가 한 열로 전환되고 제목·본문이 영역 밖으로 넘치지 않으며 Footer까지 정상 표시됨.
- 다음 검증: `pnpm check`, `pnpm build:cloudflare`, 정적 HTML/메타·Sitemap 확인 후 체크포인트 저장 및 GitHub main 푸시.

## 추가 확인

최신 데스크톱 캡처에서는 공통 헤더 검색 아래에 간결한 소개 블록과 6개 카테고리가 바로 이어지고, 추천 생활정보·최신 콘텐츠·생활 계산기·운영 원칙·Footer 순서가 유지되었다. 기존 큰 히어로 이미지와 두 번째 검색 폼은 제거되었지만 생활비랩의 큰 제목, 테라코타 밑줄, 딥 블루 추천 영역, 카드형 편집 리듬은 유지되었다.

최신 375px 모바일 캡처에서는 공통 검색이 한 번만 노출되고 소개·카테고리·추천 카드·최신 목록·계산기 카드가 세로로 안정적으로 배치되었다. 제목과 설명은 카드 영역 밖으로 넘치지 않았고, 하단 운영 원칙과 Footer까지 빈 영역 없이 표시되었다.

## 최종 순서 확인

최신 데스크톱·모바일 캡처에서 홈페이지의 실제 시각 순서가 `헤더 → 검색 스트립 → 6개 카테고리 카드 → 추천 생활정보 → 최신 콘텐츠 → 생활 계산기 → 운영 원칙 → Footer`로 완벽히 정렬되었다. 기존의 별도 소개 블록(`home-intro`)을 제거하고 브랜드 메시지를 카테고리 섹션 상단 설명으로 흡수하여, 사용자가 처음 방문했을 때 불필요한 중복 정보 없이 바로 분야를 선택할 수 있도록 간결해졌다.

## 정적 DOM 감사

최종 `dist/public/index.html`에서 `header-search-bar`, `category-section`, `featured-section`, `latest-section`, `calculator-section`, `trust-section` 클래스의 위치를 직접 비교했다. 위치가 모두 존재하고 순서가 `header-search-bar → category-section → featured-section → latest-section → calculator-section → trust-section`로 이어져 `dom_order=PASS`였으며, `<main>`은 1개였다.

## 최종 카드 스타일 보완 검수

계산기 목록 카드까지 제목 2줄·설명 3줄 클램프와 `h-full`을 적용했다. 데스크톱과 모바일 캡처에서 홈페이지 카테고리·추천 게시글·생활 계산기 카드가 동일한 카드 문법으로 보이고, 1.5rem 수준의 간격·패딩, 연한 테두리와 은은한 hover용 shadow가 반영되었다. 모바일에서는 카드가 한 열로 전환되고 콘텐츠가 카드 밖으로 넘치지 않았다. 헤더의 6개 메뉴와 검색 버튼도 한 줄 및 모바일 축약 상태를 유지했다.

## 최종 모바일 카드 재검수

최종 카드 보강 이후 375px 모바일에서 `/`와 `/calculators`를 다시 캡처했다. 카테고리·추천 게시글·계산기 카드가 한 열 또는 지정된 그리드로 안정적으로 배치되었고, 제목·설명 클램프와 `h-full`, 1.5rem 패딩·간격, 연한 테두리와 은은한 그림자가 유지되었다. 화면 바깥으로 튀어나오는 텍스트와 가로 스크롤은 확인되지 않았다.

## 계산기 링크 정렬 최종 검수

계산기 카드의 공통 직계 `span` 원형 규칙을 번호 배지와 액션 링크에 분리했다. 최종 375px 모바일 캡처의 홈페이지와 계산기 목록에서 번호만 원형으로 표시되고 `계산해 보기`·`계산기 열기`는 원형 테두리 없이 아이콘과 함께 한 줄로 정렬되었다. 텍스트 겹침과 가로 넘침은 확인되지 않았다.

## GitHub 자동 배포 권한 점검

GitHub `gjh4842-cyber/life-cost-lab`의 Rulesets 화면에는 생성된 규칙이 없고, Branches 화면에도 classic branch protection이 설정되어 있지 않다. Collaborators and teams 화면에는 “0 collaborators have access. Only you can contribute to this repository”라고 표시되므로 소유자 계정의 저장소 접근 자체는 정상이다. 그럼에도 Git push와 branch protection API가 통합 인증에서 403을 반환하므로, 현재 문제는 main 보호 규칙이 아니라 GitHub CLI/통합 인증의 쓰기 토큰 범위 또는 연결 상태로 판단된다.

## 2026-08-28 로고 수정 배포 확인

- GitHub `main` 원격 파일에 기존 `/manus-storage/trusted-life-guide-mark_0491c4bb.png` 참조가 남아 있던 것을 확인해, `SiteHeader.tsx`를 외부 이미지 없이 `랩` 텍스트 마크를 렌더링하도록 수정했다.
- `index.css`에 `.brand__mark`의 고정 크기·원형·배경·색상·모바일 축소 규칙을 추가했다.
- 로컬 `tsc --noEmit` 및 Vitest 통과, `pnpm build:cloudflare` 통과(정적 사이트맵 31개 URL, 콘텐츠 라우트 34개 사전 렌더링).
- GitHub Contents API 동기화 후 원격 `main` 커밋: `cf659e6bbfc7584b611903b3ac89d907ffb87b6b`.
- 배포 직후 `https://life-cost-lab-site.pages.dev/` 재조회에서는 기존 로고 이미지가 계속 보였으므로 Cloudflare Pages의 GitHub 자동 배포 반영 지연 가능성이 있다. 새 배포 완료 후 홈과 `/calculators`를 다시 직접 확인해야 한다.
- 홈의 섹션 순서는 헤더 → 검색 → 6개 카테고리 → 추천 생활정보 → 최신 콘텐츠 → 생활 계산기 → 운영 원칙으로 확인했다.
- Cloudflare Pages API에서 `life-cost-lab-site`의 GitHub source(`gjh4842-cyber/life-cost-lab`, production branch `main`)와 최신 커밋 `4ed96d08762df35d463e3913ca52b7340912608e`의 배포 성공을 확인했다. 실제 프로덕션 홈과 `/calculators`에서 헤더가 `랩 생활비랩`으로 표시되고 기존 `manus-storage` 깨진 이미지 아이콘이 사라졌으며, 계산기 카드 5개와 `계산기 열기` 액션이 정상 노출되었다.
- 로고 보완 이후 375px 모바일 뷰포트에서 라이브 `https://life-cost-lab-site.pages.dev/`와 `/calculators`를 충분히 로드해 직접 캡처했다. 홈은 `랩` 로고 마크·검색 아이콘·햄버거 메뉴가 한 줄에 안정적으로 배치되고 카드가 화면 폭 안에서 정렬되었으며, 계산기 목록은 동일한 헤더와 브레드크럼을 유지한 채 콘텐츠가 가로로 넘치지 않았다. 캡처 파일은 `/home/ubuntu/upload/live-mobile-qa/home-final.png`, `/home/ubuntu/upload/live-mobile-qa/calculators-final.png`에 보관했다.
- 같은 라이브 URL을 Chromium `--window-size=375,812 --virtual-time-budget=10000 --dump-dom`으로 다시 열어 모바일 DOM도 확인했다. 홈과 계산기 목록 모두 `.brand__mark`가 이미지가 아닌 텍스트 마크이고 `aria-label="메뉴 열기"` 및 `menu-trigger`가 존재했으며, 홈에는 `추천 생활정보`, 계산기 목록에는 5개의 `계산기 열기` 액션이 존재했다. 두 문서 모두 초기 로딩 화면이 아닌 실제 콘텐츠 DOM으로 저장했다.

## 최종 문구 재확인 이후 모바일 회귀 검수

- `추천 생활정보` 표기와 검색 친화적 제목을 유지한 최종 문구 상태에서 로컬 홈과 `/calculators`를 375px 뷰포트로 다시 캡처했다.
- 두 경로 모두 기존 `랩` 로고·검색 아이콘·햄버거 메뉴·검색 스트립·브레드크럼·카드 레이아웃이 유지되었고, 제목과 설명이 화면 밖으로 넘치지 않았다.
- 이번 요청 범위에 포함되지 않은 색상·간격·섹션 구조·계산기 페이지의 시각 구성은 변경되지 않은 상태로 확인했다.


## AdSense 준비 품질 개선 검수

- 홈페이지의 대표 H1을 `생활비와 생활정보를 한곳에서 찾아보세요.`로 정리해 사이트 목적을 빠르게 이해하도록 보완했다.
- 기존 6개 카테고리와 홈페이지 섹션 순서는 유지했다.
- 계산기 목록과 각 계산기 상세 화면에 기존 `life-calculators` 카테고리 글을 연결해 계산기에서 설명 글로 이동할 수 있게 했다. 새 사실·후기·통계·가격은 추가하지 않았다.
- `/search`는 `noindex,follow`로 설정하고, 핵심 콘텐츠 페이지의 `index,follow`·canonical은 유지했다.
- 외부 저장소에 의존하던 누락 파비콘 경로를 정적 `favicon.svg`로 교체했고, 모바일 확대를 막던 `maximum-scale=1`을 제거했다.
- `pnpm check` 통과, Vitest 2개 파일·4개 테스트 통과, `pnpm run build:cloudflare` 통과, `pnpm run audit:build` 통과.
- 정적 감사 결과: HTML 34개, sitemap URL 31개, 내부 링크 오류 없음.
- 최신 375px 모바일 캡처에서 홈·계산기·대표 글·문의 페이지의 기존 반응형 흐름과 카드 넘침 여부를 회귀 확인했다.


## 콘텐츠 깊이 확장 회귀 검수

- 생활비·절약, 정부지원·복지, 자동차, 주거·가전, 여행·여가 카테고리를 각 6개 글 이상으로 확장했다. 새 글은 서로 다른 검색 의도와 확인 방법·예시·주의사항·FAQ·출처를 포함하도록 구성했다.
- 생활 계산기 5개는 기존 가이드 글과 확장 글을 함께 연결했다.
- `pnpm check`, `pnpm test`(2개 파일, 4개 테스트), `pnpm run build:cloudflare`, `pnpm run audit:build`가 통과했다.
- 정적 감사 결과: HTML 58개, sitemap URL 55개, 내부 링크 오류 없음.
- 데스크톱 주요 경로와 375px 모바일 홈·복지 카테고리·복지 글·계산기 화면에서 기존 딥 블루·웜 그레이 디자인, 헤더·검색·브레드크럼·카드 폭·큰 글자 구조가 유지되는 것을 확인했다.
- sitemap 파서가 분리된 콘텐츠 배열을 잘못 읽던 문제를 수정해 확장 글의 카테고리 URL이 잘못 생성되지 않도록 했다.
- 개인 도메인 설정 및 GitHub main·Cloudflare 자동 배포 설정은 변경하지 않았다.


## 콘텐츠 확장 후 페이지네이션 회귀 검수

- 전체 글 목록에서 페이지 번호가 `123456다음`처럼 붙어 보이던 문제를 확인하고, 기존 디자인을 해치지 않는 버튼형 간격·테두리·활성 상태를 추가했다.
- 데스크톱 전체 글 목록에서 1~6 페이지와 `다음` 링크가 분리되어 표시되며, 모바일 375px에서도 페이지 번호가 줄바꿈 가능한 버튼으로 표시되고 가로 넘침이 없음을 확인했다.

## 기사 메타 구조·계산기 역방향 링크 회귀 검수

- `Article`에 대상 독자·확인 방법·예시·주의사항·FAQ를 독립 필드로 추가하고 글 상세에서 해당 필드를 섹션으로 렌더링했다. 기존 `sections` 본문 구조와도 호환된다.
- 생활정보 글에서 관련 계산기로 이동하는 CTA가 딥 블루·연한 녹색 안내 카드로 표시되고, 계산기 정의의 `guideSlugs`를 기준으로 실제 연결된 계산기만 노출된다.
- 데스크톱과 375px 모바일에서 기사 제목·본문·CTA·관련 글이 영역을 벗어나지 않고 읽기 쉬운 줄바꿈으로 표시됨을 확인했다.
- 정부지원·복지 6개 글의 공식 출처를 보건복지부 기초연금, 정부24 지원금 신청, 복지로, 국민연금공단 조회, 고용24 중장년 서비스, 마이홈 주거복지 안내·주거급여 상세 페이지로 정리했다.


## 전체 기사 품질 필드 정규화 검수

- 기존 seed 글과 확장 글을 합치는 단계에서 모든 기사에 `audience`, `verification`, `example`, `cautions`, `faqs`, `sources` 기본값을 보장하도록 정규화했다.
- 기존 글의 내용은 삭제하지 않았고, 누락된 필드에는 해당 카테고리와 글 목적에 맞는 일반 확인 안내·주의사항·FAQ·공식 출처를 보완했다.
- 카테고리별 현황은 생활비·절약 6개, 정부지원·복지 6개, 자동차 6개, 주거·가전 6개, 여행·여가 6개, 생활 계산기 설명 글 2개다.
- 정부지원·복지 출처 URL은 보건복지부·정부24·복지로·국민연금공단·고용24·마이홈 등 실제 공식 도메인으로 구성했으며, 마이홈 주거급여 상세 URL은 브라우저에서 실제 본문이 열리는 것을 확인했다.
- `pnpm check` 통과, Vitest 10개 테스트 통과, Cloudflare 정적 빌드에서 58개 라우트 사전 렌더링·55개 Sitemap URL 생성, `audit:build` 내부 링크 감사 통과.
- 대표 기사 상세를 데스크톱·375px 모바일에서 캡처해 대상 독자·실제 확인 방법·예시·주의사항·FAQ·계산기 CTA·출처·관련 글의 세로 흐름과 가로 넘침 여부를 확인했다.


## GitHub 및 Cloudflare 반영 확인

- GitHub CLI에서 `gjh4842-cyber` 계정의 `repo` 범위 재인증을 완료했다.
- 최신 콘텐츠 품질 수정본을 `gjh4842-cyber/life-cost-lab`의 `main`에 반영했으며 원격 커밋은 `80d4c6bdd26fa97ce97b6d7326ccce7e8ba8660d`이다.
- GitHub Contents API 파일 기반 payload 방식으로 대용량 파일을 포함한 변경분을 동기화했다.
- Cloudflare Pages `https://life-cost-lab-site.pages.dev/` 홈과 대표 기사 URL이 HTTP 200으로 응답했고, 홈의 `추천 생활정보`와 대표 기사 제목 마커를 확인했다.

## SEO·얇은 페이지·성능 최종 점검

정적 사전 렌더링 결과 58개 HTML과 Sitemap 55개 URL을 확인했다. 전체 경로에 title, description, canonical, Open Graph, JSON-LD, breadcrumb, 페이지 유형에 맞는 heading 및 article 구조가 생성되며, 검색 결과 `/search`에는 `noindex,follow`가 적용된다. 정적 감사는 내부 링크 오류 없이 통과했다. 카테고리 페이지는 설명과 주요 글을 제공하고, 계산기 페이지는 계산 대상·입력값·결과 해석·주의사항·관련 정보 흐름을 제공한다. 광고 강제 배치·팝업·과도한 배너·임의 리뷰·평점·가격 데이터는 추가하지 않았다.

정적 자산 성능 점검에서는 공통 JavaScript 원본 425.6KB, gzip 127.3KB, 공통 CSS 원본 145.5KB, gzip 25.7KB로 측정되었고 페이지별 청크 분할이 적용되어 있다. 재시작된 로컬 서버에서 `/`, `/articles`, `/calculators`, `/search`가 모두 HTTP 200을 반환했으며 응답 시간은 각각 약 0.007초, 0.007초, 0.004초, 0.005초였다. 이는 로컬 정적 응답 기준이며 실제 사용자 성능은 Cloudflare·브라우저·네트워크 환경에 따라 달라질 수 있다. 데스크톱·375px 모바일 화면도 캡처로 확인했다.

최신 빌드의 `search/index.html`에서 `robots`가 `noindex,follow`로 확인되었고, `pnpm test`의 10개 테스트, TypeScript 검사, `pnpm run build:cloudflare`, `pnpm run audit:build`가 통과했다.


## 소프트 오프화이트·크림회색 테마 조정

전역 배경을 차분한 소프트 오프화이트로 정리하고, 히어로·아카이브·기사·계산기·소개 영역은 서로 가까운 크림회색과 중립색으로 조정했다. 콘텐츠 카드와 계산기 입력 영역은 순백색을 유지해 본문과 카드의 경계를 분명히 했다. 네이비 본문·청록 링크·베이지 강조색은 유지해 5060 사용자의 대비와 탐색성을 보존했다.

데스크톱 1280px과 모바일 375px에서 `/`와 `/calculators`를 캡처해 확인했으며, 헤더·검색·카드·계산기 화면 모두 색상 변경으로 인한 가로 넘침이나 텍스트 이탈 없이 표시됐다. `pnpm check`, Vitest 11개, `pnpm run build:cloudflare`, `pnpm run audit:build`가 통과했고 정적 결과는 58개 HTML, 55개 Sitemap URL, 내부 링크 오류 없음이다.
