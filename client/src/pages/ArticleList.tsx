/** Design note — 여백의 생활편집실: 아카이브 화면은 많은 글 속에서도 날짜와 분야를 먼저 읽게 한다. */
import { ArrowRight } from "lucide-react"; import { Link } from "wouter"; import { ArticleCard } from "@/components/ArticleCard"; import { PageShell } from "@/components/PageShell"; import { articles, categories } from "@/data/content";
export default function ArticleList() { return <PageShell title="최신 글" description="최근에 검토하고 정리한 생활 정보 글을 확인하세요." path="/articles"><section className="archive-hero"><div className="content-wrap"><p className="eyebrow">ALL ARTICLES</p><h1>새로 정리한<br /><span>생활 정보</span></h1><p>생활에 바로 필요한 내용을 업데이트 순서대로 모았습니다. 관심 분야를 선택해 더 빠르게 찾아보세요.</p><div className="archive-category-links">{categories.map((category) => <Link key={category.slug} href={`/category/${category.slug}`}>{category.name}<ArrowRight size={15} /></Link>)}</div></div></section><section className="content-wrap archive-content"><div className="archive-content__heading"><span>전체 {articles.length}개 글</span><p>모든 글에는 게시일과 최종 검토일을 표시합니다.</p></div><div className="article-grid">{articles.map((article) => <ArticleCard article={article} key={article.slug} />)}</div></section></PageShell>; }
