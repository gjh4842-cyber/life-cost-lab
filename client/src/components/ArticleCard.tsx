/** Design note — 생활비랩: 카드에는 분야·태그·게시일·검토일을 분명히 남겨 수백 편의 글도 빠르게 판단하도록 돕는다. */
import { ArrowUpRight } from "lucide-react"; import { Link } from "wouter"; import type { Article } from "@/data/content"; import { getCategory } from "@/data/content";
export function ArticleCard({ article, priority = false }: { article: Article; priority?: boolean }) { const category = getCategory(article.category); return <article className={`article-card h-full ${priority ? "article-card--priority" : ""}`}><div className="article-card__ribbon" data-accent={category?.accent}><span>{category?.name}</span><span>{article.readTime}</span></div><div className="article-card__content"><div className="article-card__metadata"><span className="article-card__index" aria-hidden="true">정보</span><p className="article-card__date">게시 {article.publishedAt}<br />검토 {article.reviewedAt}</p></div><p className="article-card__tag">{article.tags[0]}</p><h3 className="line-clamp-2"><Link href={`/articles/${article.slug}`}>{article.title}</Link></h3><p className="article-card__excerpt line-clamp-3">{article.excerpt}</p><Link className="article-card__link" href={`/articles/${article.slug}`}>내용 읽기 <ArrowUpRight size={18} /></Link></div></article>; }
