/** Design note — 생활비랩: 헤더 아래 검색은 긴 문장보다 실제 생활 검색어를 보여 주어 첫 탐색을 쉽게 만든다. */
import { Search } from "lucide-react"; import { useLocation } from "wouter";
export function HeaderSearchBar() { const [, setLocation] = useLocation(); const submitSearch = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const query = new FormData(event.currentTarget).get("header-search")?.toString().trim(); if (query) setLocation(`/search?q=${encodeURIComponent(query)}`); }; return <section className="header-search-bar" aria-label="생활정보 검색"><div className="content-wrap header-search-bar__inner"><p><strong>필요한 생활비 정보가 있으신가요?</strong><span>생활비, 기초연금, 엔진오일, 에어컨처럼 기억나는 단어로 찾아보세요.</span></p><form role="search" onSubmit={submitSearch}><Search size={21} aria-hidden="true" /><input name="header-search" placeholder="예: 한 달 생활비, 기초연금 대상" aria-label="사이트 검색어" /><button type="submit">검색</button></form></div></section>; }
