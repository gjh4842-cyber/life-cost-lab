# 한결생활 가이드 콘텐츠 발행 규칙

새 글은 `client/src/data/content.ts`의 `articles` 배열에 추가합니다. 글의 URL은 `slug`로 결정되므로 게시 이후에는 변경하지 않는 것을 원칙으로 합니다. 제목·요약·카테고리·발행일·최종 검토일·본문 섹션을 함께 작성해야 목록, 검색, 개별 글, 메타데이터가 같은 정보를 사용합니다.

## 글을 추가할 때 함께 확인할 항목

| 항목 | 자동 처리 | 운영자가 확인할 내용 |
|---|---|---|
| 개별 URL | `slug` 기반으로 자동 생성 | 짧고 의미 있는 영문 slug인지 확인 |
| 카테고리 목록 | `category` 값으로 자동 분류 | 알맞은 기존 카테고리를 사용했는지 확인 |
| 메인 페이지 | 최신 순서와 대표 글(`featured`)을 자동 반영 | 대표 글로 노출할지 검토 |
| 관련 글 | 같은 카테고리의 최근 글을 자동 연결 | 필요하면 같은 카테고리 글의 품질·연관성 보완 |
| XML Sitemap | 빌드 시 콘텐츠 데이터에서 자동 생성 | 실제 도메인이 `siteConfig.ts`에 입력되어 있는지 확인 |
| 정적 HTML | 빌드 시 글·카테고리 경로를 자동 사전 렌더링 | `pnpm build:cloudflare`를 실행 또는 배포 빌드로 확인 |

## URL과 품질 원칙

URL은 `/articles/영문-slug` 형식을 유지하고, 이미 공개된 주소는 수정하지 않습니다. 내용이 바뀌었을 때에는 새 글을 중복하기보다 기존 글의 `reviewedAt`과 본문을 갱신합니다. 제도·건강·법률·금융·주거처럼 기준 변동이 잦거나 개인별 판단이 중요한 주제는 공식 최신 출처를 확인하고, 글의 적용 범위가 면책 안내와 일치하도록 작성합니다.

## 배포 전 운영 설정

`client/src/data/siteConfig.ts`에 실제 `domain`, `contactEmail`, `operatorName`을 입력합니다. Google Analytics 또는 Google AdSense를 사용할 때에는 각각 발급받은 측정 ID와 게시자 클라이언트 ID를 같은 설정 파일에 추가하고, 개인정보처리방침의 실제 외부 서비스 현황도 함께 갱신합니다. 빌드 시 `robots.txt`, `sitemap.xml`, `ads.txt`가 실제 도메인과 AdSense 클라이언트 ID를 기준으로 새로 생성됩니다.
