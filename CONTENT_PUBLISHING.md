# 생활비랩 콘텐츠 발행·수정 규칙

생활비랩의 글은 `client/src/data/content.ts`의 `articles` 배열에서 관리합니다. 글의 공개 주소는 `slug`로 결정되므로, 한 번 배포한 주소는 가급적 바꾸지 않는 것을 원칙으로 합니다. 새 글에는 제목·요약·카테고리·태그·발행일·최종 검토일·본문 섹션을 함께 기록해야 합니다.

## 새 글을 추가할 때 입력할 항목

| 항목 | 역할 | 운영 시 확인할 내용 |
|---|---|---|
| `slug` | `/articles/영문-slug` 고유 URL | 이미 사용 중인 slug와 겹치지 않는지 확인 |
| `category` | 여섯 개 공식 카테고리 분류 | `living-cost`, `welfare`, `car`, `housing-appliance`, `travel-leisure`, `life-calculators` 중 하나 선택 |
| `tags` | 관련글·검색어 연결 | 독자가 실제로 검색할 짧은 단어를 2~4개 입력 |
| `searchTerms` | 자연어 검색 보완 | ‘에어컨 물이 새요’처럼 자주 쓰는 표현을 추가 |
| `publishedAt` | 최초 게시일 | 실제 공개 날짜 입력 |
| `reviewedAt` / `updatedAt` | 최신성 표시 | 공식 안내를 다시 확인한 실제 날짜 입력 |
| `coverImage` / `coverAlt` | 대표 이미지 확장 필드 | 실제 권리가 확보된 이미지와 정확한 대체텍스트만 사용 |
| `sources` | 공식 근거 표시 | 직접 확인한 기관·제조사·서비스의 실제 URL과 확인일만 입력 |
| `relatedSlugs` | 우선 관련글 지정 | 독자가 다음에 읽으면 도움이 될 실제 글만 연결 |

> 출처를 아직 확인하지 않은 글에는 임의의 기관명이나 링크를 넣지 않습니다. 출처 필드는 비워 두고, 검토 후 실제로 확인한 자료만 추가하세요.

## 글을 추가하거나 수정하면 함께 바뀌는 영역

| 영역 | 처리 방식 | 운영자가 마지막으로 볼 것 |
|---|---|---|
| 개별 글 URL | `slug` 기준으로 자동 생성 | 주소가 짧고 의미가 분명한지 확인 |
| 카테고리 목록 | `category` 값으로 자동 분류 | 카테고리가 글의 주제와 맞는지 확인 |
| 메인 페이지 | 최신 글과 `featured` 글을 자동 반영 | 대표 글을 편집 기준으로 선정했는지 확인 |
| 관련 글 | 같은 카테고리·공통 태그와 `relatedSlugs`를 바탕으로 연결 | 실제로 함께 읽을 만한 글인지 확인 |
| 검색 | 제목·요약·태그·자연어 검색어·본문을 함께 검색 | 독자가 쓸 만한 검색어를 `searchTerms`에 넣었는지 확인 |
| XML Sitemap | 빌드에서 글·카테고리·계산기·페이지네이션 URL을 자동 생성 | 실제 HTTPS 도메인이 `siteConfig.ts`에 있는지 확인 |
| 정적 HTML | 배포 빌드에서 각 공개 경로를 사전 렌더링 | `pnpm build:cloudflare`가 완료됐는지 확인 |

## 생활 계산기 추가 규칙

새 계산기는 `client/src/data/calculators.ts`에 추가합니다. `slug`, 이름, 설명, 입력 필드, 결과 설명, 주의 안내를 함께 작성하면 `/calculators/계산기-slug` 주소와 목록, Sitemap, 정적 HTML이 함께 생성됩니다. 계산 결과는 사용자가 입력한 숫자를 정리하는 참고용으로만 제공하고, 개인별 금융·세무·법률 판단을 대신하는 문구를 쓰지 않습니다.

## 자동 배포 운영 방식

GitHub 연동 Cloudflare Pages 프로젝트를 사용하면 `main` 브랜치에 새 글 또는 수정 사항을 올릴 때마다 자동으로 새 배포가 생성됩니다. 배포가 끝나면 새 글 URL, 관련글, 모바일 화면, `https://xn--v69a23sc9g7wd.kr/sitemap.xml`을 직접 확인하세요.

현재 Direct Upload 프로젝트도 유지할 수 있지만, 새 글을 자주 추가한다면 GitHub 자동 배포 프로젝트를 대표 도메인에 연결하는 방식이 반복 작업을 줄이는 데 적합합니다.
